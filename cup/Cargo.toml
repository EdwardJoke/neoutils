[package]
name = "cupcli"
version = "26.3.1"
edition = "2024"
description = "Cup is the faster `cp` command for You and Your Agent, belongs to the Neoutils project"
authors = ["EdwardJoke"]
license = "Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
indicatif = "0.18"
colored = "3"
serde_json = "1"
