[package]
name = "ramcli"
version = "26.4.0"
edition = "2024"
description = "Ram is the safer `rm` command for You and Your Agent, belongs to the Neoutils project"
authors = ["EdwardJoke"]
license = "Apache-2.0"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
colored = "3.1.1"
indicatif = "0.18.4"
ignore = "0.4.25"
globset = "0.4"
serde_json = "1"
