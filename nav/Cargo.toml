[package]
name = "navcli"
version = "26.3.1"
edition = "2024"
description = "nav - jump around, faster clone of rupa/z in Rust"
authors = ["EdwardJoke"]
license = "Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
