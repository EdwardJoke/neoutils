[package]
name = "navcli"
version = "26.4.0"
edition = "2024"
description = "Nav is the intelligent `cd` replacement in Rust, belongs to the Neoutils project"
authors = ["EdwardJoke"]
license = "Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
